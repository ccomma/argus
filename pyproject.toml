[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "argus"
version = "0.1.0"
description = "Local-first work contract and capability operating system for AI power users."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
argus = "argus.cli:main"
