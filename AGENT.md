## Local scripts

- Use `./scripts/check.sh` after code changes when possible.
- Use `./scripts/review-diff.sh` before final response.
- Keep source capability scans read-only; Argus may write `.argus/` reports and inventories, but should not modify discovered assets during Phase 3.
